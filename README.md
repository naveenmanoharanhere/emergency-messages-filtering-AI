# emergency-messages-filtering-AI
An app that reads notifications and runs them through a model to filter out the messages that require immediate attention. These notifications are pushed through DND via our app.
